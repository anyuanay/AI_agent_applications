"""Shared assets for the ontology extraction skills.

Holds the single source of truth for the dataclasses that travel through the
pipeline, plus a small helper to locate the backing ``scima`` package.
"""
